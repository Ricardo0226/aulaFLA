import turtle

Rick = turtle.Turtle()

for _ in range(12):
    for _ in range(2):
        Rick.forward(60)
        Rick.right(30)
        Rick.forward(60)
        Rick.right(150)
        Rick.forward(60)
        Rick.right(30)
        Rick.forward(60)